# DevOps NGINX Demo

## Intro

This file helps you to install nginx and show the webpage

## Install steps

sudo apt-get update
sudo apt install nginx

sudo systemctl start nginx
sudo systemctl enable nginx

Web page is static
